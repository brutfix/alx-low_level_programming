#include "main.h"
/**
* mul - mulutiples of two integers
* @x: the number first number to multiply
* @y: the seconfd number to multiply with the first number
* Return: The result of the multiplication between x person and y person
*/

int mul(int x, int y)
{
	return (x + y);
}
